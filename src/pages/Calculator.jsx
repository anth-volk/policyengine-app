export const CALCULATOR_MODES = {
  HOUSEHOLD: 'household',
  POLICY: 'policy',
};

export default function Calculator(props) {
  const { mode } = props;

  return (
    <div>
      <h1>Calculator</h1>
    </div>
  )
}